Date: 2026-06-05
TaskStatus: completed
Task scope: F22 — visual-planner 图示风格预设（claude / feishu），启动前 AskUserQuestion 选风格、全文图示统一
Current summary: 原 article-visual-planner 只有一套暖米黄调色板（隐式 claude 风格），不可切换。本次：(1) 在 visual-planner.md 新增「图示风格参数（强制统一）」说明——prompt 传入 `风格: claude|feishu`，未传默认 claude，本次产出的所有 SVG 必须统一该风格、严禁混用，风格只改配色/容器外观/圆角/箭头、不改结构。(2) 新增「图示风格预设」小节：预设 A `claude`（暖米黄牛皮纸调、虚线分组框，即原调色板固化命名）；预设 B `feishu`（参考用户截图 screenshot-20260605-161754.png 的飞书/Lark 架构图：纯白底、节点浅色填充+同色系实线描边——蓝 #EAF2FE/#5B8FF9、绿 #EAF6EC/#5BB85B、黄 #FFF7E6/#F0B429、紫 #F4ECFB/#9B6FD1、实线浅绿分组容器 #F2FAF3/#7CC47C、灰色连线 #8C8C8C、主文本 #262626/次文本 #8C8C8C 标注公式维度、节点 rx=6 容器 rx=10，整体更方正清爽）。(3) 「样本模板」从 1 个 claude 示例扩成 claude + feishu 两个，结构相同配色不同。(4) section 3「颜色」硬编码调色板改为「取自指定风格预设」。(5) commands/{review,enhance,explain}.md 在第一层启动前新增 AskUserQuestion 选风格步骤（claude/feishu，默认 claude），把 `风格: <值>` 注入 Agent 4 visual-planner 的 prompt；enhance 验证轮沿用同一风格、--resume 不重复追问。diagram-renderer 无需改（只抽取落盘，风格已固化在 SVG 源码）。文档同步 CLAUDE.md / article-plugin/CLAUDE.md / README.md / feature_list.md（F22 passes:true）。
Main gaps: 无（prompt/命令/文档层改动，自检通过）。未做真实端到端跑测验证 feishu SVG 实际观感——建议下次拿一篇文带图跑一次 /explain 选 feishu，目测配色是否贴近截图，按需微调预设 B 的色值。F20（Hermes 集成）仍 passes:false，独立任务未处理。
Next steps: 真实文章跑 /enhance 或 /explain 选 feishu 风格目测效果、微调色值；继续 F20 的 hermes smoke test 与端到端回归。
