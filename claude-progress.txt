Date: 2026-04-24
TaskStatus: completed
Task scope: F17 — 修复 /explain 输出 05-explanation.md 中的图片相对路径错误（../img/ → img/）
Current summary: 用户在 /Users/dongmao.zhang/upstream/llmwiki 下用 /explain 跑了一遍，发现 `.article-work-explain/05-explanation.md` 里 4 张 SVG 图示都引用为 `../img/diagram_N.svg`，但 `05-explanation.md` 本身就在 `.article-work-explain/` 根目录、与 `img/` 同级，`../img/...` 会越过工作目录解析到 `llmwiki/img/`，渲染时 404。根因：`source/origin.md` 在 `source/` 子目录里，那里 `../img/...` 是正确的；但 `article-explainer` 与 `commands/explain.md` 把这个写法照搬到 `05-explanation.md` 上就错位了。本次：1) 直接 sed 修复用户已生成的 05-explanation.md；2) 修 `agents/article-explainer.md` 的图示集成规则与 PDF 提取图片的路径改写说明；3) 修 `commands/explain.md` 第二层 explainer 调用指令并加上路径解释。
Main gaps: 仍未做端到端 smoke test；本次只验证了"手动 sed 替换后路径正确"，没有重新跑一遍 /explain 验证 explainer 现在真的会按新规则写。
Next steps: 下一次有英文论文/博客触发 /explain 时，作为回归用例验证 explainer 写出的 05-explanation.md 直接采用 `img/...`、无需手动 sed。
