Date: 2026-06-08
TaskStatus: completed
Task scope: F20 收尾验证 —— Hermes 三方集成端到端回归通过，feature_list F20 改 passes:true
Current summary: F20 的代码侧此前已全部提交（hermes-task.mjs + 3 个 hermes agent + aggregator 三方逻辑），本次补完三步验证：
  (1) 冒烟：`node article-plugin/scripts/hermes-task.mjs --check` exit 0（hermes Agent v0.12.0 在线响应；codex 也在）。
  (2) 端到端：用短测试稿（埋入 AI 套话 + 可疑数据 300%/INT4 + 需核实事实 GPT-3 1750亿）跑 3 个 hermes agent，各产出非空报告（01/02/03-hermes.md），证明 hermes-task.mjs 转发链路对 content/style/accuracy 三种 agent 均工作；再补跑 Claude 端 content/style（accuracy 两次都因 WebSearch 工具链 socket 断而缺席——反而成了好测试），连同 hermes 报告喂给 review-aggregator。aggregator 正确执行了刚提交的三方逻辑：① accuracy 仅 Hermes 单方 → 🔴 降级为 🟡「建议联网核实」；② content/style 的 Claude×Hermes 取较高严重级别合并；③ 输出含「Agent 间分歧（Claude × Codex × Hermes）」表格（维度/位置/各方意见/倾向采纳）；④「提速 300%」被 5 份报告共振，列为最高置信度交叉验证发现；⑤ 缺席的 Claude-accuracy 与全部 Codex 按规则略过、不报错。grep 校验报告含三方分歧小节标题 + 降级标注 + 交叉验证发现，全部 ✓。
  (3) 优雅跳过：PATH 去掉 hermes 后 `which hermes` 失败 → gate 第一步即跳过整套 Hermes；`hermes-task.mjs --check` 在 hermes 缺失时干净 exit 1（spawn ENOENT），不挂死、不阻塞主流程。
  三步全过，feature_list F20 已改 passes:true。
Main gaps: Claude accuracy-checker（opus + WebSearch）本次两次调用都触发 "socket connection closed" infra 错误而未产报告——疑似 WebSearch 工具链临时问题，与 F20 无关；aggregator 已按缺席优雅处理，不影响 F20 结论，但该 agent 的稳定性可后续关注。F19（父协调层路径化 token 回归）仍 passes:false，是独立任务、未在本次处理。
Next steps: 无 F20 遗留。可选：若 accuracy-checker socket 问题复现，单独排查 WebSearch 链路；按需收口 F19。
