Date: 2026-04-24
TaskStatus: not_completed
Task scope: F19 — 父协调层路径化输入 + /enhance loop 压到 2 轮
Current summary: 用户反馈 article-plugin 的 /review/enhance/explain 在跑长 PDF 论文时疯狂消耗 token。诊断后发现：父协调层按当前 commands/*.md 的"全文 + 图片"措辞会把 origin.md 内容和 ../img/*.png 图片以多模态 image block 形式嵌入每个并行 agent 的 prompt，5 个并行 agent 等于父 context 持有 5 份图片副本；再加上 /enhance 最多 4 轮验证循环，token 放大极其严重。本次只做两处定向修复：(1) commands/{review,enhance,explain}.md 的 layer1/explainer/rewriter 输入表全部改为"路径写法"，并显式声明"调用方不得把文件内容或图片嵌入 prompt"；(2) /enhance 验证循环上限从 N<4 改为 N<2，文件清单移除 rewrite-round-3/4。同时把 9 个 agent 的"图像输入"段统一替换为"输入加载"段，明确"调用方只传路径，agent 自行 Read，按需加载图片"，并按 agent 角色给出"是否需要 Read 图片"的差异化指引（visual-planner 全 Read，content-reviewer/style-auditor 几乎不 Read，accuracy-checker/translator/rewriter 按需）。CLAUDE.md / README.md 同步把"最多 3 轮"改为"最多 2 轮"。模型未降级、codex 副本未关闭、WebSearch 未剥离（用户明确选择最小改动 + 仅压 loop 上限的范围）。
Main gaps: 端到端真实 PDF 回归未跑——计划用 latent_action 论文先备份 .article-work-explain/ 再重跑 /explain，验证父对话 token 下降 ≥40% 且 05-explanation.md 质量无明显劣化；以及一篇 markdown 输入跑 /enhance 验证 2 轮上限是否能解决多数 🔴。回归通过后再把 feature_list.md F19 passes 标 true、claude-progress.txt TaskStatus 改 completed。
Next steps: (1) 备份现有 .article-work-explain/ 再重跑 /explain <latent_action.pdf>，对比父对话 token / 各 agent token / 05-explanation.md 字数与质量；(2) 选一篇 markdown 跑 /enhance 验证 2 轮上限；(3) 通过后更新 feature_list.md F19 passes:true、claude-progress.txt TaskStatus:completed，git commit 验证产物。
