Date: 2026-06-05
TaskStatus: completed
Task scope: F21 — article-style-auditor 内置确定性 AI 味词典（命中即必改），解决"AI 味仍浓、靠模型自由判断漏检率高"的问题
Current summary: 经 web search 汇总常见 AI 味词条（ChatGPT/AIGC 高频词、中文套话与商业黑话、Humanizer-zh 24 模式、英文 AI 直译词如 delve/tapestry/leverage/robust），在 agents/article-style-auditor.md 新增「AI 味词典（命中即判定 AI 味，务必去除）」小节，分 5 类黑名单：A 套话连接词/过渡词、B 商业/互联网黑话、C AI 高频形容词/副词/动词、D 模板化开头/结尾/标题（短语模式）、E 英文 AI 直译词。指示 opus 版审计前先用 Grep 逐条扫描主文件，任何命中一律列入 🔴 严重 AI 味（必须改写）并给自然替换；词典是底线、模型自由判断是补充，二者取并集。含「真术语豁免」细则（向量维度、模型鲁棒性/robust、端到端训练等确属术语者保留但须标注依据），审查摘要要求注明「词典命中 N 处」且 N 影响 AI 味评分。同步把同一份词典写入 article-style-auditor-hermes.md 的 grounding_rules「六、AI 味词典」，使 Hermes 第三方视角也强制执行（Hermes 无 Grep，按 prompt 内联扫描）。文档同步：CLAUDE.md / article-plugin/CLAUDE.md / README.md / feature_list.md（新增 F21，passes:true）。
Main gaps: 无（prompt/文档层改动，按词条覆盖与措辞自检通过）。注意 F20（Hermes 集成）仍在 feature_list 标 passes:false，其 smoke test / 端到端回归独立于本任务，未在本次处理。
Next steps: 下次会话可继续 F20 的 hermes-task.mjs smoke test 与 /explain 端到端回归；或在真实文章上跑一次 /enhance 验证词典实际去 AI 味效果、按命中情况微调词条。
